Place the images you provided here:
- logo.png  (small logo, transparent background recommended)
- hero.jpg  (hero image, recommended 1920x1080 or similar)

If you uploaded the images to the repo UI, move them into this folder so `index.html` can reference them at `assets/logo.png` and `assets/hero.jpg`.
